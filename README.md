# csv_data_pipeline
A custom Python data pipeline that cleans a purposely unstructured Titanic dataset using native libraries, including type validation, median/mode imputation, deduplication, and automated unit tests.
